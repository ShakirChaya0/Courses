#include <stdio.h>
#include <math.h>

int main() {

  double A = sqrt(9);      // Squareroot of a number
  double B = pow(2.4, 4);  // Power of a number
  int C = round(3.14);     // Rounde a number
  int D = ceil(3.01);      // Round up
  int E = floor(3.99);     // Round down
  double F = fabs(-100);   // Absolute value
  double G = log(3);       // Log of a number
  double H = sin(45);      // Sin of a number
  double I = cos(45);      // Cos of a number
  double J = tan(45);      // Tan of a number
  
  return 0;
}
